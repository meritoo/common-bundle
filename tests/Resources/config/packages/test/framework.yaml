framework:
    test: true
    session:
        storage_id: session.storage.mock_file
    translator:
        default_path: '%kernel.project_dir%/../src/Resources/translations'
        fallbacks:
            - '%locale%'
